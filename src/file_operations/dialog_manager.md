```mermaid
classDiagram
class DialogManager {
    +ask_directory()
    +ask_output_location()
    +on_button_click(button_id)
    +ask_yes_no(title, message)
    +root
    +directory
    +selected_option
    +dialog
    +label
    +button_frame
    +options
    +selected_option
    +button
    +answer
}
```
